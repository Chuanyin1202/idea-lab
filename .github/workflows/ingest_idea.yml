name: Ingest IdeaBrowser Idea

on:
  # 每日 UTC 02:00 執行（台灣時間 10:00）
  schedule:
    - cron: "0 2 * * *"

  # 支援手動觸發（測試用）
  workflow_dispatch:

jobs:
  ingest:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run ingest script
        env:
          # OpenAI API Key
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

          # Gmail API - 方案 1: Service Account JSON (Base64 編碼)
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}

          # Gmail API - 方案 2: Service Account 檔案路徑（如果使用檔案上傳）
          # GOOGLE_APPLICATION_CREDENTIALS: ${{ secrets.GOOGLE_APPLICATION_CREDENTIALS }}

        run: |
          cd ${{ github.workspace }}
          python scripts/ingest_idea.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # 檢查是否有變更
          if [ -n "$(git status --porcelain)" ]; then
            echo "✅ 偵測到新的 idea，準備提交..."

            git add ideas/ SUMMARY.md

            # 取得新增的 idea 標題（從最新的目錄名稱）
            LATEST_IDEA=$(ls -td ideas/*/ | head -1 | xargs basename)
            echo "📝 新增 idea: $LATEST_IDEA"

            git commit -m "chore: add daily idea - $LATEST_IDEA

🤖 Auto-generated by IdeaBrowser ingestion workflow"

            git push
            echo "✅ 推送成功"
          else
            echo "ℹ️  沒有新的 idea"
          fi

      - name: Notify on failure
        if: failure()
        run: |
          echo "❌ Workflow 執行失敗"
          echo "請檢查："
          echo "  1. OPENAI_API_KEY 是否正確"
          echo "  2. GOOGLE_SERVICE_ACCOUNT_JSON 是否正確"
          echo "  3. Gmail API 權限是否足夠"
